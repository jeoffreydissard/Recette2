{% block footer %}
    {% if pagination is defined %}
    <footer>
        <div class="container">
            <div class="navigation">
                {{ knp_pagination_render(pagination) }}
            </div>
        </div>
    </footer>
    {% endif %}
{% endblock %}
